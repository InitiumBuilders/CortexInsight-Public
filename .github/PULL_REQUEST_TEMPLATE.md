## TITLE

One line: what this changes.

## CONFIDENCE

n/10. BASIS: the evidence this number rests on, one line.

## FALSIFIER

One observable, with a clock: if X is not seen by Y, this was wrong, because Z.

## SHUTTLE

The one lived turn you walked through your change, in order: a real operator, a real path. If that turn is not better, say so.

## DID

Precisely what changed. Name the deletion alternative you considered.

## FILES

The paths you edited.

## GATE

- [ ] `node --check main.js`
- [ ] `npm run smoke` and the report read (paste the lines that matter)
- [ ] `npm run clicktest` if a start path was touched
- [ ] `npm run fresh` if a view, the gate, or first-run behaviour was touched
- [ ] `npm run gate` exits 0
- [ ] screenshots at desktop and 375 px, looked at, for anything visual
- [ ] any new instrument feeds an actor and has a smoke fixture

## NEXT

The single best follow-up, or none.
